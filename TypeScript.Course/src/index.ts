console.log("Create tsconfig.json file")
//const NName = "jaddu"

function NName(dataName:string)
{
    
    console.log(dataName)
}
print()

//HOw to make tsconfig.json --> tsc --init
//rootDIr set how -->src
//outDir set how -->./dist
//unUsedLocals ->true it will error if you don't use that variable
//NoEmmitError --> true if error ouccer in .ts file than you can't emmit code.